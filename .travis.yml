#user container-based infrastructure
sudo: false
before_install:
# install dependencies
install: "pip install -r requirements.txt"
language: python
python:
  - "2.7"
script: robot -L TRACE --variable ENV:dev -d result testcases/web